spring:
  application:
    name: "help-desk-backend"
  #datasource configurations
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/helpdesk
    username: root
    password: papa9829
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  ai:
    openai:
      api-key: ${Groq_API_KEY}
      chat:
        options:
          model: llama-3.1-8b-instant
      base-url: https://api.groq.com/openai


    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
  profiles:
    active: dev


server:
  port: 8081


#database configurations

another:
  student: true
